<!DOCTYPE html>
<html lang="en">
    <body>
        @extends('components.head')
        @extends('components.navbar')
        @yield('content')
        @extends('components.footer')
        @extends('components.scripts')
    </body>
</html>

